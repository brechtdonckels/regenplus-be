---
const { logoImg, logoAltText, partnerDescription, partnerUrl } = Astro.props;
---

<div
  class="items-top mb-12 flex flex-row space-x-8 text-base sm:space-x-12 sm:text-lg lg:space-x-16"
>
  <a href={partnerUrl}>
    <img
      src={logoImg}
      alt={logoAltText}
      class="mx-auto w-24 basis-1/5 sm:w-28"
    />
  </a>
  <p class="basis-4/5">{partnerDescription}</p>
</div>
